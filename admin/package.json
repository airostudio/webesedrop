{
  "name": "dropship-engine-admin",
  "version": "0.1.0",
  "private": true,
  "description": "Operator-only admin dashboard for the dropship engine: subscription billing/accounting, the domain install log, and drill-down reports. Talks to the engine only over its /v1/admin/* REST API (see ../README.md) — no shared code, no shared database.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.11"
  }
}
