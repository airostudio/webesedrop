{
  "name": "@trend/db",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "schema": "echo 'Apply supabase/schema.sql via the Supabase SQL editor, or: supabase db execute -f supabase/schema.sql'",
    "seed": "echo 'Apply supabase/seed.sql via the Supabase SQL editor, or: supabase db execute -f supabase/seed.sql'",
    "types": "supabase gen types typescript --project-id \"$SUPABASE_PROJECT_ID\" --schema public > src/database.types.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0"
  }
}
