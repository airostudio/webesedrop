/**
 * Placeholder for generated Supabase types. Once a project is provisioned
 * and `supabase/schema.sql` applied, regenerate this file with:
 *
 *   pnpm -C packages/db types
 *   (requires SUPABASE_PROJECT_ID and the Supabase CLI logged in)
 *
 * Until then, `Database` is untyped so the client compiles without a live
 * project — callers should still type query results explicitly at the call
 * site (see apps/web/lib/types.ts for the shapes used by the storefront).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
